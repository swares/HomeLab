# Homelab TODO — 2026-07-14

Audit findings from codebase review. Grouped by priority.

---

## 🔴 High — Correctness / Reliability

- [ ] **Remove duplicate log collector** — Alloy and Promtail are both deployed and both
  pushing to Loki. Every log line is doubled. Decide which to keep and remove the other.
  `gitops/apps/alloy.yaml` · `gitops/apps/promtail.yaml`

- [ ] **Add ExternalSecret for Authelia** — `authelia-secrets` (6 keys: jwt_secret,
  session_secret, storage_encryption_key, oidc_hmac_secret, ldap_password,
  storage_pg_password + oidc_private_key) must be created manually before ArgoCD can sync
  Authelia. No ExternalSecret exists unlike every other workload.
  `gitops/workloads/authelia/` (missing external-secret.yaml)

- [ ] **Fix GitLab runner TLS placeholder** — literal `REPLACE_WITH_BASE64_CA_CERT` is
  committed at line 20. Runner cannot verify GitLab TLS cert until replaced.
  `gitops/workloads/gitlab-runner/gitlab-tls-secret.yaml:20`

- [ ] **Add resource limits/requests** — no `resources:` blocks on any of these containers.
  OOM on the H4 will cascade unpredictably:
  - `gitops/workloads/ai-gateway/deployment.yaml` (litellm)
  - `gitops/workloads/authelia/deployment.yaml` (authelia × 2)
  - `gitops/workloads/authelia/postgres.yaml` (authelia-postgres)
  - `gitops/workloads/home-assistant/deployment.yaml` (home-assistant)
  - `gitops/workloads/immich/server.yaml` (immich-server)
  - `gitops/workloads/immich/machine-learning.yaml` (immich-ml)
  - `gitops/workloads/immich/postgres.yaml` (immich-postgres)
  - `gitops/workloads/immich/redis.yaml` (immich-redis)
  - `gitops/workloads/lldap/deployment.yaml` (lldap)

- [ ] **Add liveness/readiness probes** — missing on:
  - `gitops/workloads/ai-gateway/deployment.yaml` (litellm — both missing)
  - `gitops/workloads/immich/machine-learning.yaml` (both missing)
  - `gitops/workloads/immich/postgres.yaml` (both missing)
  - `gitops/workloads/immich/redis.yaml` (both missing)
  - `gitops/workloads/immich/server.yaml` (liveness missing; readiness present)

---

## 🟡 Medium — Operational Hygiene

<<<<<<< Updated upstream
- [ ] **Pin LiteLLM image tag** — `ghcr.io/berriai/litellm:main-latest` is a mutable
  floating tag from the default branch. Pin to a versioned release.
  `gitops/workloads/ai-gateway/deployment.yaml:21`
=======
- [x] **Pin LiteLLM image tag** — pinned to `v1.85.7` (latest on GHCR; `v1.86.2` tag doesn't exist). *(PR open)*
  `gitops/workloads/ai-gateway/deployment.yaml`
>>>>>>> Stashed changes

- [ ] **Pin lldap image tag** — `lldap/lldap:stable` is floating. Pin to `vX.Y.Z`.
  `gitops/workloads/lldap/deployment.yaml`

- [ ] **Pin Immich image tags** — `immich-server`, `immich-ml`, and the postgres image
  all use `:release`. Server and ML must stay in sync; postgres suffix must match.
  `gitops/workloads/immich/server.yaml` · `machine-learning.yaml` · `postgres.yaml`

- [x] **Pin Reloader to a tagged release** — pinned to `chart-v2.2.14` (appVersion v1.4.19). *(PR open)*
  `gitops/workloads/reloader/kustomization.yaml`

- [ ] **Fix Grafana Loki datasource `$${__value.raw}` bug** — double-dollar is a Helm
  escape idiom and is invalid in a raw ConfigMap. Grafana receives it verbatim.
  Change to `${__value.raw}`.
  `gitops/workloads/monitoring/loki-datasource.yaml`

- [ ] **Add Zot registry authentication** — no `auth:` block in Zot config. Any pod or
  host with network access can push images; other workloads pull from this registry.
  `gitops/workloads/registry/configmap.yaml`

- [ ] **Add NetworkPolicies** — zero NetworkPolicy objects exist. At minimum add
  least-privilege policies for the auth namespace (Authelia + lldap) and ai-gateway.

- [ ] **Add PodDisruptionBudgets for immich-postgres and lldap** — a node drain will
  delete the photos DB or the LDAP server. Only Authelia has a PDB today.

- [ ] **Fix site.yml — replace microshift with k3s** — `ansible/playbooks/site.yml`
  imports `microshift.yml`; `k3s-h4.yml` is never reachable from `make all`.
  `ansible/playbooks/site.yml`

- [ ] **Upgrade rknpu driver to 0.9.7 on both OPis** — 0.9.6 blocks all 3B+ NPU models
  (Llama-3.2-3B, Qwen2.5-3B). Upgrade via `make ai-nodes` after driver is available.
  `gitops/workloads/ai-gateway/configmap.yaml` (TODO comment)

- [ ] **Fix GitHub Actions dry-run-apply** — `validate.yml` runs `kubectl apply
  --dry-run=server` on a hosted runner with no LAN access. Server API at
  `192.168.1.200` is unreachable. Move job to self-hosted runner or convert to
  client-side dry-run. `.github/workflows/validate.yml`

- [ ] **Resolve duplicate CI systems** — both `.gitlab-ci.yml` and `.github/workflows/`
  implement validate/sync-check/rollback. GitLab is more complete; GitHub sync-check
  is a no-op stub. Pick one or clearly document which is authoritative.

- [x] **Add ExternalSecret for m5stack-auth** — ESO-managed, Vault path
  `secret/lab/m5stack`. Secret synced and pod healthy. *(PR open)*

---

## 🟢 Low — Cleanup

- [ ] **Gitignore rollout-report.txt** — generated CI artifact committed to repo root.
  Add to `.gitignore` and remove the file.

- [ ] **Update CLAUDE.md — RPi 4B role** — says RPi 4B runs OpenLDAP; it actually runs
  Home Assistant. lldap is a KVM VM (`ldap-1`) on n150-1.

- [ ] **Disable or remove sample-app** — `gitops/apps/sample-app.yaml` deploys a demo
  httpd pod with `selfHeal: true` in production. Red Hat registry image may rate-limit.

- [ ] **Consistent targetRevision in reloader app** — uses `HEAD` while every other
  Application uses `main`. `gitops/apps/reloader.yaml`

- [ ] **Add TLS to Grafana ingress** — `grafana.ini root_url` is HTTP and the ingress
  has no TLS annotation unlike every other service. Admin creds cross LAN in cleartext.
  `gitops/apps/monitoring.yaml`

- [ ] **Fix or remove broken Makefile targets** — `bake-image` references missing
  `packer/proxmox.pkrvars.hcl`; `k3s-vms` references missing `terraform/` directory.
  Add `.pkrvars.hcl.example` or remove the targets. `Makefile`

- [ ] **Add example config files** — `ansible/files/ovms/config.json.example` referenced
  in `ai-nodes.yml` comments does not exist. `packer/proxmox.pkrvars.hcl.example`
  also missing.

- [ ] **Remove stale `oc` commands from .claude/settings.json** — allow list includes
  OpenShift CLI commands (`oc get`, `oc describe`, etc.) for a k3s cluster.

- [ ] **Document manual bootstrap steps** — `authelia-secrets` and `m5stack-auth` are
  now both ESO-managed (populate Vault, then ESO syncs). Remaining gap: document the
  full Vault-first bootstrap sequence in `docs/RUNBOOK.md`.

- [ ] **Fix vestigial `route.yaml` naming** — three ingress files are named `route.yaml`
  (argocd, ai-gateway, immich) — holdover from MicroShift era. Rename to `ingress.yaml`.

- [ ] **Update immich/README.md** — says "Runs on the H4 MicroShift node". Cluster is k3s.
  `gitops/workloads/immich/README.md:1`

- [ ] **Update RUNBOOK.md for k3s** — references MicroShift commands and backup/restore
  flows. Needs k3s equivalents (etcd snapshot restore, not `microshift-backup`).
