# Homelab TODO — 2026-07-14

Audit findings from codebase review. Grouped by priority.

---

## 🔴 High — Correctness / Reliability

- [x] **Remove duplicate log collector** — Kept Alloy (Promtail removed). Added
  `unit` + `hostname` journal labels to Alloy to match Promtail output.
  `gitops/apps/alloy.yaml` · `gitops/apps/promtail.yaml` *(PR merged)*

- [x] **Add ExternalSecret for Authelia** — `external-secret.yaml` added, Vault populated,
  hand-created secret deleted. ESO owns `authelia-secrets`, pods stayed running.
  `gitops/workloads/authelia/external-secret.yaml` *(PR merged)*

- [x] **Fix GitLab runner TLS placeholder** — already corrected before this audit;
  cert was in place. `gitops/workloads/gitlab-runner/gitlab-tls-secret.yaml`

- [x] **Add resource limits/requests** — all 9 containers now have requests + limits. *(PR open)*
  - `gitops/workloads/ai-gateway/deployment.yaml` (litellm)
  - `gitops/workloads/authelia/deployment.yaml` (authelia × 2)
  - `gitops/workloads/authelia/postgres.yaml` (authelia-postgres)
  - `gitops/workloads/home-assistant/deployment.yaml` (home-assistant)
  - `gitops/workloads/immich/server.yaml` (immich-server)
  - `gitops/workloads/immich/machine-learning.yaml` (immich-ml)
  - `gitops/workloads/immich/postgres.yaml` (immich-postgres)
  - `gitops/workloads/immich/redis.yaml` (immich-redis)
  - `gitops/workloads/lldap/deployment.yaml` (lldap)

- [x] **Add liveness/readiness probes** — all 5 gaps filled. *(PR open)*
  - `gitops/workloads/ai-gateway/deployment.yaml` (litellm — both added)
  - `gitops/workloads/immich/machine-learning.yaml` (both added)
  - `gitops/workloads/immich/postgres.yaml` (both added)
  - `gitops/workloads/immich/redis.yaml` (both added)
  - `gitops/workloads/immich/server.yaml` (liveness added)

---

## 🟡 Medium — Operational Hygiene

- [x] **Pin LiteLLM image tag** — pinned to `main-v1.86.2`. *(PR open)*
  `gitops/workloads/ai-gateway/deployment.yaml`

- [x] **Pin lldap image tag** — pinned to `v0.6.3` (confirmed running version). *(PR open)*
  `gitops/workloads/lldap/deployment.yaml`

- [x] **Pin Immich image tags** — server + ml pinned to `v2.7.5`. *(PR open)*
  `gitops/workloads/immich/server.yaml` · `machine-learning.yaml`

- [ ] **Pin Reloader to a tagged release** — `kustomization.yaml` pulls from a raw
  `master` branch URL. Any breaking upstream push is applied immediately.
  `gitops/workloads/reloader/kustomization.yaml`

- [x] **Fix Grafana Loki datasource `$${__value.raw}` bug** — fixed to `${__value.raw}`.
  `gitops/workloads/monitoring/loki-datasource.yaml` *(PR merged)*

- [ ] **Add Zot registry authentication** — no `auth:` block in Zot config. Any pod or
  host with network access can push images; other workloads pull from this registry.
  `gitops/workloads/registry/configmap.yaml`

- [ ] **Add NetworkPolicies** — zero NetworkPolicy objects exist. At minimum add
  least-privilege policies for the auth namespace (Authelia + lldap) and ai-gateway.

- [ ] **Add PodDisruptionBudgets for immich-postgres and lldap** — a node drain will
  delete the photos DB or the LDAP server. Only Authelia has a PDB today.

- [x] **Fix site.yml — replace microshift with k3s** — corrected to `k3s-h4.yml`,
  added comment block listing standalone playbooks.
  `ansible/playbooks/site.yml` *(PR open)*

- [ ] **Upgrade rknpu driver to 0.9.7 on both OPis** — 0.9.6 blocks all 3B+ NPU models
  (Llama-3.2-3B, Qwen2.5-3B). Upgrade via `make ai-nodes` after driver is available.
  `gitops/workloads/ai-gateway/configmap.yaml` (TODO comment)

- [ ] **Fix GitHub Actions dry-run-apply** — `validate.yml` runs `kubectl apply
  --dry-run=server` on a hosted runner with no LAN access. Server API at
  `192.168.1.200` is unreachable. Move job to self-hosted runner or convert to
  client-side dry-run. `.github/workflows/validate.yml`

- [ ] **Resolve duplicate CI systems** — both `.gitlab-ci.yml` and `.github/workflows/`
  implement validate/sync-check/rollback. GitLab is more complete; GitHub sync-check
  is a no-op stub. Pick one or clearly document which is authoritative.

- [ ] **Add ExternalSecret for m5stack-auth** — `gitops/workloads/ai-gateway/
  m5stack-adapter/deployment.yaml` references `m5stack-auth` secret created manually
  via `kubectl create secret generic`. Needs ESO backing like other secrets.

---

## 🟢 Low — Cleanup

- [x] **Gitignore rollout-report.txt** — added to `.gitignore`; file removed. *(PR open)*

- [x] **Update CLAUDE.md — RPi 4B role** — corrected: RPi 4B runs Home Assistant;
  lldap is KVM VM (`ldap-1`) on n150-1. *(PR open)*

- [x] **Disable or remove sample-app** — `gitops/apps/sample-app.yaml` and
  `gitops/workloads/sample-app/` removed. *(PR open)*

- [x] **Consistent targetRevision in reloader app** — `HEAD` → `main`. *(PR open)*
  `gitops/apps/reloader.yaml`

- [x] **Add TLS to Grafana ingress** — added cert-manager/lab-ca annotations, TLS block,
  updated `root_url` to https. `gitops/apps/monitoring.yaml` *(PR merged)*

- [x] **Fix or remove broken Makefile targets** — added prereq notes to `bake-image`,
  `k3s-vms`, and `ldap` targets. `Makefile` *(PR open)*

- [ ] **Add example config files** — `ansible/files/ovms/config.json.example` referenced
  in `ai-nodes.yml` comments does not exist. `packer/proxmox.pkrvars.hcl.example`
  also missing.

- [ ] **Remove stale `oc` commands from .claude/settings.json** — `.claude/settings.json`
  is write-protected in Cowork; remove `oc` entries manually.

- [ ] **Document manual bootstrap steps** — no runbook for creating `authelia-secrets`
  or `m5stack-auth` before first ArgoCD sync. Add to `docs/RUNBOOK.md` or inline.

- [x] **Fix vestigial `route.yaml` naming** — renamed to `ingress.yaml` in argocd,
  ai-gateway, and immich workload dirs. *(PR open)*

- [x] **Update immich/README.md** — "MicroShift node" → "k3s node". *(PR open)*
  `gitops/workloads/immich/README.md`

- [ ] **Update RUNBOOK.md for k3s** — references MicroShift commands and backup/restore
  flows. Needs k3s equivalents (etcd snapshot restore, not `microshift-backup`).
