apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: argocd
data:
  # Run argocd-server without TLS — Traefik terminates TLS at the ingress edge.
  # Without this, ArgoCD redirects HTTP→HTTPS internally causing redirect loops.
  server.insecure: "true"
