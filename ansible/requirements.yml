---
# Ansible Galaxy collections required by this repo.
# Install with: ansible-galaxy collection install -r ansible/requirements.yml
# or: make ansible-deps
collections:
  - name: community.general
    version: ">=9.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
