---
zswap_enabled: true
zswap_compressor: lz4
zswap_max_pool_percent: 20

# Swapfile location and size — override per host/group
zswap_swapfile_path: /swapfile
zswap_swapfile_size_mb: 2048

# Set true on hosts that use zram for swap (OPi 5 Pros)
zswap_disable_zram: false
