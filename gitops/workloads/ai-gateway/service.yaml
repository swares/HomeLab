apiVersion: v1
kind: Service
metadata:
  name: litellm
  namespace: ai-gateway
  labels:
    app: litellm
spec:
  selector: { app: litellm }
  ports:
    - name: http
      port: 4000
      targetPort: 4000
