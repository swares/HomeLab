apiVersion: apps/v1
kind: Deployment
metadata:
  name: minio
  namespace: minio
spec:
  replicas: 1
  selector:
    matchLabels: { app: minio }
  strategy:
    type: Recreate   # RWO PVC — only one pod can mount at a time
  template:
    metadata:
      labels: { app: minio }
    spec:
      nodeSelector:
        kubernetes.io/hostname: n150-1   # NVMe-backed; keep state close to other PVCs
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: minio
          # renovate: datasource=docker depName=minio/minio
          image: minio/minio:RELEASE.2025-04-22T22-12-26Z
          args: ["server", "/data", "--console-address", ":9001"]
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: minio-credentials
                  key: root-user
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: minio-credentials
                  key: root-password
            # OIDC SSO via Authelia — users log in once and land in MinIO Console.
            # After first login, run:
            #   mc admin policy attach local consoleAdmin --user "<preferred_username>"
            # Use internal HTTP URL to avoid lab-CA TLS verification.
            # The issuer in the discovery doc (https://authelia.apps.lab.home.arpa)
            # is still used for the browser-facing OIDC flow.
            - name: MINIO_IDENTITY_OPENID_CONFIG_URL
              value: http://authelia-svc.authelia.svc.cluster.local:9091/.well-known/openid-configuration
            - name: MINIO_IDENTITY_OPENID_CLIENT_ID
              value: minio
            - name: MINIO_IDENTITY_OPENID_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: minio-credentials
                  key: oidc-client-secret
            - name: MINIO_IDENTITY_OPENID_REDIRECT_URI
              value: https://minio-console.apps.lab.home.arpa/oauth_callback
            - name: MINIO_IDENTITY_OPENID_SCOPES
              value: openid,profile,email
            - name: MINIO_IDENTITY_OPENID_CLAIM_NAME
              value: preferred_username
            - name: MINIO_IDENTITY_OPENID_DISPLAY_NAME
              value: Authelia
          ports:
            - name: api
              containerPort: 9000
            - name: console
              containerPort: 9001
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: [ALL] }
          readinessProbe:
            httpGet: { path: /minio/health/ready, port: 9000 }
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet: { path: /minio/health/live, port: 9000 }
            initialDelaySeconds: 30
            periodSeconds: 30
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: minio-data
